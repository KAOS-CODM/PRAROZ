class SearchBar extends HTMLElement{constructor(){super(),this.attachShadow({mode:"open"}),this.shadowRoot.innerHTML=`
            <style>
    .search-container {
        display: flex;
        justify-content: center; /* Centers horizontally */
        gap: 10px;
        width: 60%; /* Adjust width to keep it centered */
        max-width: 500px; /* Prevent it from stretching too much */
        margin: 20px auto; /* Centers it with equal space on both sides */
    }
    
    input {
        flex-grow: 1;
        padding: 12px; /* Increased height */
        border: 1px solid #ccc;
        border-radius: 6px;
        font-size: 16px; /* Slightly larger text */
    }
    
    button {
        padding: 12px 16px; /* Increased height */
        background-color: #007BFF;
        color: white;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-size: 16px;
    }

    button:hover {
        background-color: #0056b3;
    }

    /* Responsive for 760px */
    @media (max-width: 760px) {
        .search-container {
            width: 80%; /* Wider for better fit */
        }
        
        input, button {
            font-size: 14px; /* Slightly smaller text */
            padding: 10px;
        }
    }

    /* Responsive for 480px */
    @media (max-width: 480px) {
        .search-container {
            width: 90%; /* Almost full width */
            flex-direction: column; /* Stack input and button vertically */
        }

        button {
            width: 100%; /* Full width for better usability */
        }
    }
</style>

            <div class="search-container">
                <input type="text" id="searchInput" placeholder="Search recipe or category...">
                <button id="searchButton">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <circle cx="11" cy="11" r="8"></circle>
                        <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
                    </svg>
                </button>
            </div>
         `}connectedCallback(){var e=this.shadowRoot.getElementById("searchInput"),t=this.shadowRoot.getElementById("searchButton");e.addEventListener("input",()=>this.performSearch()),t.addEventListener("click",()=>this.performSearch())}performSearch(){let t=this.shadowRoot.getElementById("searchInput").value.toLowerCase().trim();var e,o=document.querySelectorAll(".recipe-item"),r=document.querySelectorAll(".recipe-card-link"),n=document.querySelectorAll("h2");let i=!1,l=null;if(""===t)n.forEach(e=>e.style.display="block"),o.forEach(e=>e.style.display="block"),r.forEach(e=>e.style.display="block"),(e=document.getElementById("noResultsMessage"))&&e.remove();else{n.forEach(e=>e.style.display="none"),o.forEach(e=>e.style.display="none"),r.forEach(e=>e.style.display="none"),n.forEach(e=>{e.textContent.toLowerCase().includes(t)&&((l=e).style.display="block",i=!0)}),o.forEach(e=>{if((e.querySelector("h3")?.textContent.toLowerCase()||"").includes(t))e.style.display="block",i=!0;else if(l){let e=l.nextElementSibling;for(;e&&e.classList.contains("recipe-item");)e.style.display="block",e=e.nextElementSibling}}),r.forEach(e=>{(e.querySelector(".recipe-title")?.textContent.toLowerCase()||"").includes(t)&&(e.style.display="block",i=!0)});let e=document.getElementById("noResultsMessage");i?e&&e.remove():e||((e=document.createElement("p")).id="noResultsMessage",e.textContent="No matching recipes or categories found.",document.getElementById("recipe-list")?.appendChild(e))}}}customElements.define("search-bar",SearchBar);